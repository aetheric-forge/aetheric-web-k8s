apiVersion: v1
kind: Service

metadata:
  name: aethericforge-web
  namespace: aetheric-forge

spec:
  type: ClusterIP

  selector:
    app: aethericforge-web

  ports:
    - name: http
      port: 80
      targetPort: 8080